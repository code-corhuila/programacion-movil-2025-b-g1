import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angel.nazoru',   // el ID de tu app (puede ser otro pero mantenlo igual siempre)
  appName: 'Nazoru',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
