import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'new-project-angular-ionic',
  webDir: 'www'
};

export default config;
